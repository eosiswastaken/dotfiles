#
# wm independent hotkeys
#

# terminal emulator
alt + Return
	alacritty

# program launcher
alt + space
	rofi -show drun

# restart polybar
alt + BackSpace
	killall polybar && /home/eosis/.config/polybar/launch.sh &